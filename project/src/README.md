# Исходный код проекта

Структура:

- `data/` - скачивание и чтение датасета;
- `features/` - список признаков и подготовка данных;
- `models/` - обучение, сохранение модели и инференс;
- `service/` - FastAPI-приложение.

Основные команды:

```bash
python -m src.train
python -m src.service
```
