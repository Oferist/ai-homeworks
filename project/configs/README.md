# Конфигурационные файлы

- `config.yaml` - основные настройки проекта: пути к данным, split, признаки, гиперпараметры моделей и параметры сервиса.
- `.env.example` - пример переменных окружения без реальных секретов.

Реальный `.env` не должен попадать в репозиторий.
